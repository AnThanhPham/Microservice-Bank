spring:
  application:
    name: "configserver"
  profiles:
    active: "native"
  cloud:
    config:
      server:
#        native:
#          search-locations: "classpath:/config"
#          search-locations: "file:///F:/config"
        git:
          uri: "https://github.com/AnThanhPham/atp-config"
          default-label: main
          timeout: 5
          clone-on-start: true
          force-pull: true

server:
  port: 8071